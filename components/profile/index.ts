import ProfileHeader, { ProfileHeaderProps, ProfileStat } from './ProfileHeader';
import AthleteProfileCard, { AthleteProfileCardProps, AthleteMetric } from './AthleteProfileCard';

export {
  ProfileHeader,
  ProfileHeaderProps,
  ProfileStat,
  AthleteProfileCard,
  AthleteProfileCardProps,
  AthleteMetric
};

export default {
  ProfileHeader,
  AthleteProfileCard
};
