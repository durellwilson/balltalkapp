/**
 * Layout components index
 * 
 * This file exports all layout components, making it easier to import
 * multiple components in other files.
 */

import ProfileLayout, { ProfileLayoutProps } from './ProfileLayout';

// Export components
export { ProfileLayout };

// Export types
export type { ProfileLayoutProps };

// Export a default object with all components
export default {
  ProfileLayout,
};
